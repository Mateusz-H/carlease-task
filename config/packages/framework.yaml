framework:
    secret: '%env(APP_SECRET)%'
    csrf_protection: true
    http_method_override: false
    php_errors:
        log: true
    session:
        handler_id: null
        cookie_secure: auto
        cookie_samesite: lax
